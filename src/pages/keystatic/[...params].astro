---
import { makePage } from '@keystatic/astro/ui';
import keystaticConfig from '../../../keystatic.config';

export const prerender = false;

const params = Astro.params;
---

<Fragment set:html={await makePage(keystaticConfig, params)} />



